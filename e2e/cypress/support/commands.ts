// import { User } from '../interfaces';
// import { createUser } from '../requests';

// function createNewUser(): Cypress.Chainable<User> {
//   return cy.request(createUser()).then((response) => response.body.data.createUser);
// }

// Cypress.Commands.add('createNewUser', createNewUser);

// declare global {
//   namespace Cypress {
//     interface Chainable {
//       createNewUser: typeof createNewUser;
//     }
//   }
// }

// export {};
